---
phase: 17-fix-non-ui-code-review-findings-architecture-concurrency-data-model
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/newsthread/app/domain/model/Story.kt
  - app/src/main/java/com/newsthread/app/domain/model/Article.kt
  - app/src/main/java/com/newsthread/app/domain/model/Source.kt
  - app/src/main/java/com/newsthread/app/domain/model/SourceRating.kt
  - app/src/main/java/com/newsthread/app/domain/model/TrackedStory.kt
  - app/src/main/java/com/newsthread/app/domain/repository/TrackingRepository.kt
  - app/src/main/java/com/newsthread/app/domain/repository/TextExtractionPort.kt
  - app/src/main/java/com/newsthread/app/domain/usecase/GetSimilarArticlesUseCase.kt
  - app/src/main/java/com/newsthread/app/data/repository/TrackingRepositoryImpl.kt
  - app/src/main/java/com/newsthread/app/data/repository/TextExtractionRepository.kt
  - app/src/main/java/com/newsthread/app/di/RepositoryModule.kt
autonomous: true

must_haves:
  truths:
    - "Domain models have no Android framework imports (no Parcelable)"
    - "Domain layer has no imports from data.local package"
    - "TrackedStory references Story domain model, not StoryEntity"
    - "TrackingRepository returns Flow<List<TrackedStory>>, not StoryWithArticles"
    - "GetSimilarArticlesUseCase depends on TextExtractionPort interface, not concrete class"
  artifacts:
    - path: "app/src/main/java/com/newsthread/app/domain/model/Story.kt"
      provides: "Framework-free Story domain model"
      contains: "data class Story"
    - path: "app/src/main/java/com/newsthread/app/domain/repository/TextExtractionPort.kt"
      provides: "Domain interface for text extraction"
      contains: "interface TextExtractionPort"
  key_links:
    - from: "TrackingRepositoryImpl"
      to: "Story domain model"
      via: "StoryEntity → Story mapper"
      pattern: "fun.*toStory|toStoryDomain"
    - from: "TextExtractionRepository"
      to: "TextExtractionPort"
      via: "implements interface"
      pattern: "TextExtractionRepository.*:.*TextExtractionPort"
---

<objective>
Remove all Clean Architecture boundary violations from the domain layer.

Purpose: Make the domain layer framework-free and data-layer-independent, preparing for future multi-module architecture. Per user decision: full cleanup, not pragmatic shortcuts.
Output: Clean domain models, new Story domain model, TextExtractionPort interface, updated TrackingRepository interface.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/gsd-execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-fix-non-ui-code-review-findings-architecture-concurrency-data-model/17-CONTEXT.md
@.planning/phases/17-fix-non-ui-code-review-findings-architecture-concurrency-data-model/17-RESEARCH.md
@app/src/main/java/com/newsthread/app/domain/model/Article.kt
@app/src/main/java/com/newsthread/app/domain/model/Source.kt
@app/src/main/java/com/newsthread/app/domain/model/SourceRating.kt
@app/src/main/java/com/newsthread/app/domain/model/TrackedStory.kt
@app/src/main/java/com/newsthread/app/domain/repository/TrackingRepository.kt
@app/src/main/java/com/newsthread/app/domain/usecase/GetSimilarArticlesUseCase.kt
@app/src/main/java/com/newsthread/app/data/repository/TrackingRepositoryImpl.kt
@app/src/main/java/com/newsthread/app/data/repository/TextExtractionRepository.kt
@app/src/main/java/com/newsthread/app/di/RepositoryModule.kt
@app/src/main/java/com/newsthread/app/data/local/entity/StoryEntity.kt
@app/src/main/java/com/newsthread/app/data/local/dao/StoryDao.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Story domain model and TextExtractionPort interface</name>
  <files>
    app/src/main/java/com/newsthread/app/domain/model/Story.kt
    app/src/main/java/com/newsthread/app/domain/repository/TextExtractionPort.kt
  </files>
  <action>
    1. **Create `domain/model/Story.kt`** — New data class mirroring StoryEntity fields but framework-free:
       ```kotlin
       data class Story(
           val id: String,
           val title: String,
           val createdAt: Long,
           val updatedAt: Long,
           val lastViewedAt: Long,
           val lastCheckedAt: Long,
           val lastNotifiedAt: Long,
           val hasUnseenUpdates: Boolean
       )
       ```

    2. **Create `domain/repository/TextExtractionPort.kt`** — Minimal interface:
       ```kotlin
       interface TextExtractionPort {
           suspend fun extractByUrl(url: String): ExtractionResult
       }
       ```
       Import `ExtractionResult` from `domain.model`.
  </action>
  <verify>Build compiles with new files: `./gradlew assembleDebug`</verify>
  <done>Story domain model and TextExtractionPort interface exist in domain layer with no framework/data imports.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Parcelable and wire domain boundary fixes</name>
  <files>
    app/src/main/java/com/newsthread/app/domain/model/Article.kt
    app/src/main/java/com/newsthread/app/domain/model/Source.kt
    app/src/main/java/com/newsthread/app/domain/model/SourceRating.kt
    app/src/main/java/com/newsthread/app/domain/model/TrackedStory.kt
    app/src/main/java/com/newsthread/app/domain/repository/TrackingRepository.kt
    app/src/main/java/com/newsthread/app/domain/usecase/GetSimilarArticlesUseCase.kt
    app/src/main/java/com/newsthread/app/data/repository/TrackingRepositoryImpl.kt
    app/src/main/java/com/newsthread/app/data/repository/TextExtractionRepository.kt
    app/src/main/java/com/newsthread/app/di/RepositoryModule.kt
  </files>
  <action>
    1. **Article.kt** — Remove `@Parcelize`, `import android.os.Parcelable`, `import kotlinx.parcelize.Parcelize`, and `: Parcelable`. Keep the data class clean.

    2. **Source.kt** — Same removal of Parcelable annotations and imports.

    3. **SourceRating.kt** — Same removal. Keep the helper methods (`getBiasSymbol`, etc.).

    4. **TrackedStory.kt** — Replace `import StoryEntity` with `import Story`. Change `val story: StoryEntity` to `val story: Story`. Update `unreadCount` getter to use `it.publishedAt` directly (will be Long after Plan 04, for now keep toLongOrNull guard as publishedAt is still String).

    5. **TrackingRepository.kt** — Remove `import StoryWithArticles`. Change `getTrackedStories()` return type from `Flow<List<StoryWithArticles>>` to `Flow<List<TrackedStory>>`. Import `TrackedStory` from domain.model.

    6. **TrackingRepositoryImpl.kt** — Add mapper function `StoryEntity.toStory(): Story`. Update `getTrackedStories()` implementation to map `StoryWithArticles` → `TrackedStory` using the mapper. The mapping:
       - `StoryWithArticles.story` (StoryEntity) → `story.toStory()` (Story domain model)
       - `StoryWithArticles.articles` (List<CachedArticleEntity>) → map to `List<Article>` using existing article mappers

    7. **GetSimilarArticlesUseCase.kt** — Change constructor param from `TextExtractionRepository` to `TextExtractionPort`. Update import.

    8. **TextExtractionRepository.kt** — Add `: TextExtractionPort` to class declaration.

    9. **RepositoryModule.kt** — Add `@Binds` for `TextExtractionPort`:
       ```kotlin
       @Binds @Singleton
       abstract fun bindTextExtractionPort(impl: TextExtractionRepository): TextExtractionPort
       ```

    10. **Fix all compilation errors** — Any ViewModel or screen that was passing Article via Parcelable nav args now needs to pass article URL as String and load from repository. Check:
        - Navigation routes in MainActivity/NavHost
        - ComparisonScreen nav args
        - ArticleDetailScreen nav args
        - FeedViewModel.loadTrackedStories() which uses StoryWithArticles
  </action>
  <verify>
    Run `./gradlew assembleDebug` — build must succeed.
    Grep domain/model/ for "Parcelable" — must return 0 results.
    Grep domain/ for "data.local" — must return 0 results.
    Grep GetSimilarArticlesUseCase for "TextExtractionPort" — must find it.
  </verify>
  <done>
    Domain layer is fully framework-free. No Parcelable, no StoryEntity, no StoryWithArticles in domain. TextExtractionPort wired via Hilt.
  </done>
</task>

</tasks>

<verification>
- `./gradlew assembleDebug` passes
- `grep -r "Parcelable" app/src/main/java/com/newsthread/app/domain/` returns nothing
- `grep -r "data.local" app/src/main/java/com/newsthread/app/domain/` returns nothing
- `grep -r "TextExtractionPort" app/src/main/java/com/newsthread/app/domain/usecase/` finds GetSimilarArticlesUseCase
- All existing tests still pass: `./gradlew test`
</verification>

<success_criteria>
Domain layer has zero framework dependencies. TrackingRepository returns domain types. TextExtractionPort properly abstracted. Build and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-non-ui-code-review-findings-architecture-concurrency-data-model/17-02-SUMMARY.md`
</output>

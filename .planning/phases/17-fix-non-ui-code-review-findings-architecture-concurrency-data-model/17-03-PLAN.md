---
phase: 17-fix-non-ui-code-review-findings-architecture-concurrency-data-model
plan: 03
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - app/src/main/java/com/newsthread/app/worker/BackgroundWorkScheduler.kt
  - app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt
  - app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt
  - app/src/main/java/com/newsthread/app/domain/repository/SourceRatingRepository.kt
  - app/src/main/java/com/newsthread/app/data/repository/SourceRatingRepositoryImpl.kt
autonomous: true

must_haves:
  truths:
    - "BackgroundWorkScheduler uses injected @ApplicationScope CoroutineScope, not self-created scope"
    - "TrackingViewModel.refresh() has no delay() call"
    - "ComparisonViewModel uses combine() with ratings Flow, not point-in-time query inside collect"
    - "SourceRatingRepository exposes a Flow for ratings"
  artifacts:
    - path: "app/src/main/java/com/newsthread/app/worker/BackgroundWorkScheduler.kt"
      provides: "Properly scoped coroutines with error handling"
      contains: "@ApplicationScope"
    - path: "app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt"
      provides: "Optimistic refresh without hardcoded delay"
    - path: "app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt"
      provides: "Efficient ratings caching via Flow combine"
      contains: "combine"
  key_links:
    - from: "BackgroundWorkScheduler"
      to: "CoroutinesModule"
      via: "@ApplicationScope injection"
      pattern: "@ApplicationScope.*CoroutineScope"
---

<objective>
Fix concurrency anti-patterns identified in the code review audit.

Purpose: Replace hardcoded delay with optimistic UI, inject app-scoped CoroutineScope (already exists in CoroutinesModule!), and restructure ComparisonViewModel to use combine() for ratings.
Output: 3 ViewModels/workers with proper concurrency patterns.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/gsd-execute-plan.md
</execution_context>

<context>
@.planning/phases/17-fix-non-ui-code-review-findings-architecture-concurrency-data-model/17-CONTEXT.md
@.planning/phases/17-fix-non-ui-code-review-findings-architecture-concurrency-data-model/17-RESEARCH.md
@app/src/main/java/com/newsthread/app/worker/BackgroundWorkScheduler.kt
@app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt
@app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt
@app/src/main/java/com/newsthread/app/di/CoroutinesModule.kt
@app/src/main/java/com/newsthread/app/domain/repository/SourceRatingRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject @ApplicationScope into BackgroundWorkScheduler and remove TrackingViewModel delay</name>
  <files>
    app/src/main/java/com/newsthread/app/worker/BackgroundWorkScheduler.kt
    app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt
  </files>
  <action>
    1. **BackgroundWorkScheduler.kt** —
       - Add constructor param: `@ApplicationScope private val applicationScope: CoroutineScope`
       - Remove line 30: `private val scope = CoroutineScope(Dispatchers.Default + SupervisorJob())`
       - Replace all `scope.launch` with `applicationScope.launch`
       - Add `CoroutineExceptionHandler` to the launch block:
         ```kotlin
         private val exceptionHandler = CoroutineExceptionHandler { _, throwable ->
             Log.e(TAG, "Background work error", throwable)
         }
         ```
       - Use: `applicationScope.launch(exceptionHandler) { ... }`
       - Add import for `@ApplicationScope` from `com.newsthread.app.di.ApplicationScope`
       - Add `companion object { private const val TAG = "BackgroundWorkScheduler" }`

    2. **TrackingViewModel.kt** —
       - In `refresh()` function (lines 63-76):
         - Remove `delay(2000)` (line 71)
         - Set `_isRefreshing.value = false` immediately after `workManager.enqueue(request)`
         - Remove `import kotlinx.coroutines.delay`
       - The Room DAO Flow will automatically update `trackedStories` when StoryUpdateWorker writes new data.
  </action>
  <verify>
    Run `./gradlew assembleDebug` — build succeeds.
    Grep BackgroundWorkScheduler for "CoroutineScope(Dispatchers" — must return 0 results.
    Grep TrackingViewModel for "delay(" — must return 0 results.
  </verify>
  <done>
    BackgroundWorkScheduler uses injected scope with error handling. TrackingViewModel uses optimistic UI pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure ComparisonViewModel to use combine() for ratings</name>
  <files>
    app/src/main/java/com/newsthread/app/domain/repository/SourceRatingRepository.kt
    app/src/main/java/com/newsthread/app/data/repository/SourceRatingRepositoryImpl.kt
    app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt
  </files>
  <action>
    1. **SourceRatingRepository.kt** — Add a Flow-based method:
       ```kotlin
       fun getAllSourcesFlow(): Flow<List<SourceRating>>
       ```
       Keep existing `suspend fun getAllSources()` for backward compatibility.

    2. **SourceRatingRepositoryImpl.kt** — Implement `getAllSourcesFlow()` by exposing the Room DAO's Flow query directly and mapping entities to domain models.

    3. **ComparisonViewModel.kt** —
       - Remove the `val allRatings = sourceRatingRepository.getAllSources()` call from inside the `collect` block (line 68).
       - Instead, load ratings once in init or lazily:
         ```kotlin
         private val ratingsFlow = sourceRatingRepository.getAllSourcesFlow()
             .stateIn(viewModelScope, SharingStarted.Lazily, emptyList())
         ```
       - In `findSimilarArticles()`, use `ratingsFlow.value` instead of calling `getAllSources()`.
       - This avoids re-querying Room on every comparison while keeping the data reactive if it ever changes.
  </action>
  <verify>
    Run `./gradlew assembleDebug` — build succeeds.
    Grep ComparisonViewModel for "getAllSources()" — must NOT appear inside collect block.
    Grep for "ratingsFlow" or "getAllSourcesFlow" in ComparisonViewModel — must find it.
    Run `./gradlew test` — all existing tests pass.
  </verify>
  <done>
    ComparisonViewModel no longer re-queries ratings on every comparison. Ratings cached via StateFlow.
  </done>
</task>

</tasks>

<verification>
- `./gradlew assembleDebug` passes
- `./gradlew test` passes
- No `delay(` in TrackingViewModel
- No self-created CoroutineScope in BackgroundWorkScheduler
- ComparisonViewModel uses cached ratings via Flow
</verification>

<success_criteria>
All 3 concurrency fixes applied. Build and all tests pass. No hardcoded delays, no unscoped coroutines, no redundant Room queries.
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-non-ui-code-review-findings-architecture-concurrency-data-model/17-03-SUMMARY.md`
</output>

---
id: 19-01
wave: 1
autonomous: true
objective: Optimize Tracking screen load time from 8s to <1s using a lightweight summary model and fix counter labels.
---

# Plan 19-01: Performance & Tracking Logic

## Objective
Reduce `TrackingScreen` load time by implementing an optimized data flow and fixing the story update counters.

## Tasks
- [ ] **Data Model**: Create `TrackedStorySummary.kt` in `domain.model`.
- [ ] **DAO**: Add `@Query` to `StoryDao.kt` to fetch `TrackedStorySummary` with article counts in a single SQL aggregation.
- [ ] **Repository**: Update `TrackingRepository.kt` to expose `getTrackedStorySummaries(): Flow<List<TrackedStorySummary>>`.
- [ ] **ViewModel**: Update `TrackingViewModel.kt` to use the new lightweight flow.
- [ ] **UI**: Update `EnhancedStoryCard` in `TrackingScreen.kt` to display `${unreadCount} NEW · ${totalCount} total`.

## Verification
- Manual: Tracking screen should load in under 1 second.
- Manual: Counters should accurately reflect new articles versus total articles in a thread.

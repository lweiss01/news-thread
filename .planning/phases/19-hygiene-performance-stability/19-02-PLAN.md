---
id: 19-02
wave: 2
autonomous: true
objective: Fix ArticleCard layout jitter using Red/Green TDD, revert FAB styles, and fix navigation regression.
---

# Plan 19-02: UI Stability & Polish (TDD)

## Objective
Stabilize the `ArticleCard` UI, ensure internal navigation, and revert UI elements to high-visibility standards.

## Tasks
- [ ] **ArticleCard (TDD)**:
    - **Red**: Run app and observe the layout shift in `ArticleCard` when toggling track status.
    - **Green**: Apply `widthIn(min = 80.dp)` to the `TextButton` in `ArticleCard.kt` to stabilize the action area.
- [ ] **Navigation**:
    - Update `MatchedArticleCard.kt` to accept an `onClick` callback.
    - Pass this callback from `ComparisonScreen.kt` to `MainActivity` via `NewsThreadApp` to open the internal WebView.
- [ ] **FAB Styles**:
    - Revert `FloatingActionButton` in `StoryDetailScreen.kt` and `ComparisonScreen.kt` from glassmorphic to standard Material 3 style.
- [ ] **Modernization**:
    - Replace Accompanist `SwipeRefresh` with Material 3 `PullToRefreshContainer` in `TrackingScreen.kt`.

## Verification
- TDD: Verify the layout no longer "jumps" when toggling the bookmark/track icon.
- Manual: Matched articles should open in the app's WebView.
- Manual: FABs should be highly visible and standard M3 style.

# Plan 15-01: Cloudflare Worker Setup & RSS Normalization

Initialize the Cloudflare Worker environment and implement the core RSS fetching and normalization logic using Hono and TypeScript.

## Proposed Changes

### Configuration

#### [NEW] [wrangler.json](file:///c:/Users/lweis/Documents/newsthread/worker/wrangler.json)
Configure the worker name, compatibility date, and KV namespace for caching.

#### [NEW] [package.json](file:///c:/Users/lweis/Documents/newsthread/worker/package.json)
Standard Worker dependencies: `hono`, `wrangler`, `fast-xml-parser`.

### Cloudflare Worker

#### [NEW] [index.ts](file:///c:/Users/lweis/Documents/newsthread/worker/src/index.ts)
Implement the Hono router and the `/feeds/top-stories` endpoint.

#### [NEW] [rss.ts](file:///c:/Users/lweis/Documents/newsthread/worker/src/rss.ts)
Port the RSS parsing logic from the Android app (Kotlin) to TypeScript.

#### [NEW] [types.ts](file:///c:/Users/lweis/Documents/newsthread/worker/src/types.ts)
Define the `Article` and `FeedItem` TypeScript interfaces to match the Android domain models.

## Verification Plan

### Automated Tests
- **Unit Tests**: Test `rss.ts` parsing logic with sample XML payloads.

### Manual Verification
1. Run `npx wrangler dev` in the `worker/` directory.
2. Curl `http://localhost:8787/feeds/top-stories` and verify the JSON response contains normalized articles with thumbnails and correctly parsed dates.

# Plan 15-02: Worker URL Resolution & KV Caching

Implement the server-side Google News URL resolution and integrate Workers KV to cache resolved article URLs, ensuring the resolver is only hit once per article.

## Proposed Changes

### Cloudflare Worker

#### [MODIFY] [index.ts](file:///c:/Users/lweis/Documents/newsthread/worker/src/index.ts)
Implement `/feeds/category/:category` and `/feeds/search` endpoints.

#### [NEW] [resolver.ts](file:///c:/Users/lweis/Documents/newsthread/worker/src/resolver.ts)
Port the `batchexecute` URL resolution logic to TypeScript.
- Resolve Google News redirects server-side.
- Check KV cache before attempting resolution.
- Store results in KV with a 7-day TTL.

#### [MODIFY] [wrangler.json](file:///c:/Users/lweis/Documents/newsthread/worker/wrangler.json)
Bind the `URL_CACHE` KV namespace to the worker.

## Verification Plan

### Automated Tests
- **Unit Tests**: Test `resolver.ts` with known Google News redirect URLs.

### Manual Verification
1. Curl a category feed (e.g., `/feeds/category/technology`).
2. Verify that the returned article URLs are the direct outlet URLs (e.g., `nytimes.com/...`), not `news.google.com/...`.
3. Check the Cloudflare dashboard (or use `wrangler kv:key list`) to verify URLs are being cached.
4. Verify subsequent requests are faster as they hit the KV cache.

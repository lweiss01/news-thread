---
phase: 15-cloudflare-backend
task: 4
total_tasks: 4
status: blocked
last_updated: 2026-02-22T07:33:18.767Z
---

<current_state>
We are currently debugging a persistent "Redirect Notice" / "invalid web address" issue with Google News articles. 
Despite implementing Chrome User-Agent spoofing in the app and canonical URL normalization in the worker, Lisa is still reporting an "invalid web address" error in the WebView.
</current_state>

<completed_work>
- **Worker Level**:
  - Upgraded `resolver.ts` with advanced base64 decoding (prefix/suffix stripping, length byte handling).
  - Added CAPTCHA detection to worker resolution strategies.
  - Implemented URL normalization: unresolved RSS links are normalized to `https://news.google.com/articles/CBMi...` (canonical path) to bypass one redirect hop.
  - Verified worker deployment and unit tests pass.
- **App Level**:
  - Updated `ArticleDetailScreen.kt` to set a modern Chrome User-Agent in the `WebView` settings.
- **Investigation**:
  - Confirmed via browser subagent that raw links resolve correctly in a standard browser.
  - Identified that `AU_yqL` format URLs require `batchexecute`, which is often blocked for Cloudflare IPs.
</completed_work>

<remaining_work>
- **Investigate "invalid web address" error**:
  - Verify if the normalized URL produced by the worker is actually valid.
  - Check if the `WebView` needs additional settings like `domStorageEnabled` (already on) or enabling third-party cookies.
  - Verify if the "invalid web address" is coming from Google News or the publisher site.
- **Diagnostics**:
  - Log the final article URL in `ArticleDetailScreen.kt` to see exactly what is being loaded when it fails.
- **Verification**:
  - Deploy worker again if any further changes are needed (Lisa asked if she needed to deploy).
</remaining_work>

<decisions_made>
- **Decided to normalize to /articles/**: The RSS path triggers more hops. Normalizing to the canonical path improves success in browsers.
- **Decided to spoof Chrome UA**: Generic WebViews are often flagged; a modern Chrome UA string helps bypass basic bot detection.
</decisions_made>

<blockers>
- **Persistent "invalid web address" error**: The combination of worker fix + app fix didn't fully resolve the issue for Lisa.
</blockers>

<context>
The "Vibe": We've done the "correct" engineering fixes but Google's anti-bot measures are extremely aggressive for these specific `AU_yqL` dynamic links. The fallback to a canonical URL plus UA spoofing should have worked, so there might be a subtle malformation in the URL or a missing session state/cookie in the `WebView`.
</context>

<next_action>
Start with: Log the `articleUrl` in `ArticleDetailScreen.kt` just before `loadUrl(articleUrl)` to verify the exact string being passed to the WebView. Then test that exact URL in a desktop browser.
</next_action>

# Plan: 11-03 Comparison Screen (Stream)

## Goal
Redesign the `ComparisonScreen` to the "Stream" concept, removing segmented headers and implementing a unified list with clear visual bias indicators.

## Proposed Changes

### UI Components
#### [MODIFY] [MatchedArticleCard.kt](file:///C:/Users/lweis/Documents/newsthread/app/src/main/java/com/newsthread/app/presentation/comparison/MatchedArticleCard.kt)
- Add bias indicator:
    - Vertical colored bar (4dp width) on the left edge.
    - Color driven by source bias (Left = Blue, Right = Red, Center = Grey).
- Simplify card content:
    - Remove heavy borders if present.
    - Emphasize Headline and Source.
    - Add "Hybrid Match" badge (if relevant from Phase 9.5).

#### [MODIFY] [ComparisonScreen.kt](file:///C:/Users/lweis/Documents/newsthread/app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonScreen.kt)
- Remove `PerspectiveHeader` composable.
- Flatten list structure:
    - Merge `left`, `center`, `right` articles into a single list sorted by Time.
    - Or keep grouped but remove headers and just rely on visual indicators?
    - *Decision from Design Doc:* Unified stream sorted by relevance/time + Grouping by major updates if applicable. Let's start with Time-based sort of all perspectives.
- **Header:** Add large interactive `BiasHeatmap` at the top as a summary.

### Logic
#### [MODIFY] [ComparisonViewModel.kt](file:///C:/Users/lweis/Documents/newsthread/app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt)
- Update `ComparisonUiState` to provide a flattened list of `DisplayArticle` (wrapper with bias color info) instead of separate lists?
- Or just do it in the UI layer. UI layer is fine for now.

## Verification Plan

### Manual Verification
- **Stream Visualization:** Verify articles from different perspectives appear in the same list, distinguishable by their edge color.
- **Heatmap Interaction:** Verify the large heatmap at the top correctly reflects the density of the articles below.
- **Scroll Performance:** Ensure the flat list scrolls smoothly with many items.

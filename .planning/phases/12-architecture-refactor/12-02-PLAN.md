---
phase: 12-architecture-refactor
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt
  - app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt
  - app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt
  - app/src/main/java/com/newsthread/app/presentation/MainActivity.kt
  - app/src/main/java/com/newsthread/app/util/DatabaseSeeder.kt
autonomous: true

must_haves:
  truths:
    - "FeedViewModel uses GetSourceRatingsMapUseCase instead of direct sourceRatingRepository"
    - "FeedViewModel uses ToggleFollowUseCase instead of inline follow/unfollow logic"
    - "FeedViewModel has no dead dependencies (userPreferencesRepository removed)"
    - "TrackingViewModel extends ViewModel (not AndroidViewModel) with injected @ApplicationContext"
    - "TrackingViewModel uses GetSourceRatingsMapUseCase instead of direct sourceRatingRepository"
    - "ComparisonViewModel uses GetSourceRatingsMapUseCase instead of direct sourceRatingRepository"
    - "MainActivity injects DatabaseSeeder via Hilt instead of manual construction"
    - "App builds and runs correctly with ./gradlew assembleDebug"
  artifacts:
    - path: "app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt"
      provides: "Refactored FeedViewModel with UseCase dependencies"
      contains: "GetSourceRatingsMapUseCase"
    - path: "app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt"
      provides: "Refactored TrackingViewModel extending ViewModel"
      contains: "class TrackingViewModel @Inject constructor"
    - path: "app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt"
      provides: "Refactored ComparisonViewModel using GetSourceRatingsMapUseCase"
      contains: "GetSourceRatingsMapUseCase"
    - path: "app/src/main/java/com/newsthread/app/presentation/MainActivity.kt"
      provides: "MainActivity with Hilt-injected DatabaseSeeder"
      contains: "@Inject lateinit var databaseSeeder"
    - path: "app/src/main/java/com/newsthread/app/util/DatabaseSeeder.kt"
      provides: "DatabaseSeeder with @ApplicationContext annotation"
      contains: "@ApplicationContext"
  key_links:
    - from: "FeedViewModel"
      to: "ToggleFollowUseCase"
      via: "constructor injection"
      pattern: "toggleFollowUseCase"
    - from: "FeedViewModel + TrackingViewModel + ComparisonViewModel"
      to: "GetSourceRatingsMapUseCase"
      via: "constructor injection"
      pattern: "getSourceRatingsMapUseCase"
    - from: "MainActivity"
      to: "DatabaseSeeder"
      via: "Hilt field injection"
      pattern: "@Inject.*databaseSeeder"
---

<objective>
Standardize ViewModel dependencies to use UseCases (where appropriate), fix TrackingViewModel's AndroidViewModel pattern, and clean up MainActivity's manual DI.

Purpose: Complete the architecture refactor by ensuring ViewModels follow the pragmatic rule (UseCases for domain logic, direct repos OK for simple reads) and all dependency injection goes through Hilt.

Output: 3 refactored ViewModels, 1 cleaned-up MainActivity, 1 fixed DatabaseSeeder. No behavior changes.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-architecture-refactor/12-CONTEXT.md
@.planning/phases/12-architecture-refactor/12-RESEARCH.md
@.planning/phases/12-architecture-refactor/12-01-SUMMARY.md
@app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt
@app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt
@app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt
@app/src/main/java/com/newsthread/app/presentation/MainActivity.kt
@app/src/main/java/com/newsthread/app/util/DatabaseSeeder.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ViewModels to Use Domain UseCases</name>
  <files>
    app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt
    app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt
    app/src/main/java/com/newsthread/app/presentation/comparison/ComparisonViewModel.kt
  </files>
  <action>
    **FeedViewModel refactor:**
    1. Add constructor param: `private val getSourceRatingsMapUseCase: GetSourceRatingsMapUseCase`
    2. Add constructor param: `private val toggleFollowUseCase: ToggleFollowUseCase`
    3. Remove constructor param: `private val sourceRatingRepository: SourceRatingRepository` (replaced by UseCase)
    4. Remove constructor param: `private val userPreferencesRepository: UserPreferencesRepository` (dead dependency — never used in ViewModel code)
    5. Replace `loadSourceRatings()` method body: instead of collecting from `sourceRatingRepository.getAllSourcesFlow()` and building the map manually, call `val map = getSourceRatingsMapUseCase()` and set `_sourceRatings.value = map`
    6. Replace `toggleFollow()` method body: instead of the inline if/else with `trackingRepository.unfollowStory()` / `followStoryUseCase()`, call `toggleFollowUseCase(article, _trackedStoriesMap.value)`
    7. Remove direct `followStoryUseCase` constructor param (now used via `ToggleFollowUseCase`)
    8. Keep `trackingRepository` — still needed for `loadTrackedStories()` (per CONTEXT.md decision: leave until duplicated)
    9. Keep `newsRepository` — data access, acceptable per pragmatic rule
    10. Keep `quotaRepository` — simple data read, acceptable per pragmatic rule
    11. Clean up unused imports

    **TrackingViewModel refactor:**
    1. Change `AndroidViewModel(application)` to `ViewModel()`
    2. Remove constructor param: `application: Application`
    3. Add constructor param: `@ApplicationContext private val context: Context`
    4. Add constructor param: `private val getSourceRatingsMapUseCase: GetSourceRatingsMapUseCase`
    5. Remove constructor param: `private val sourceRatingRepository: SourceRatingRepository`
    6. Replace `loadSourceRatings()` body: call `val map = getSourceRatingsMapUseCase()` and set `_sourceRatings.value = map`
    7. Update `refresh()`: change `WorkManager.getInstance(getApplication())` to `WorkManager.getInstance(context)`
    8. Keep `trackingRepository` — needed for `markStoryViewed()`, `markBadgeSeen()`, `removeArticleFromStory()`
    9. Clean up imports: remove `android.app.Application`, `AndroidViewModel`; add `Context`, `ApplicationContext`, `ViewModel`

    **ComparisonViewModel refactor:**
    1. Add constructor param: `private val getSourceRatingsMapUseCase: GetSourceRatingsMapUseCase`
    2. Remove constructor param: `private val sourceRatingRepository: SourceRatingRepository`
    3. Replace `loadSourceRatings()` body: call `val map = getSourceRatingsMapUseCase()` and set `_sourceRatings.value = map`
    4. Keep `userPreferencesRepository` — used for hint message logic (simple data read, acceptable)
    5. Keep `networkMonitor` — infrastructure (acceptable)
    6. Clean up imports
  </action>
  <verify>
    Run `./gradlew compileDebugKotlin 2>&1 | Select-String -Pattern "error"` — should show 0 errors.
    Grep FeedViewModel for dead dependency: `Select-String -Path app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt -Pattern "userPreferencesRepository"` — should return nothing.
    Grep TrackingViewModel for old pattern: `Select-String -Path app/src/main/java/com/newsthread/app/presentation/tracking/TrackingViewModel.kt -Pattern "AndroidViewModel|getApplication"` — should return nothing.
    Grep all 3 ViewModels for old sourceRatingRepository: `Select-String -Path app/src/main/java/com/newsthread/app/presentation/*/ComparisonViewModel.kt,app/src/main/java/com/newsthread/app/presentation/*/TrackingViewModel.kt,app/src/main/java/com/newsthread/app/presentation/*/FeedViewModel.kt -Pattern "sourceRatingRepository"` — should return nothing.
  </verify>
  <done>
    FeedViewModel depends on GetSourceRatingsMapUseCase and ToggleFollowUseCase. Dead userPreferencesRepository removed.
    TrackingViewModel extends ViewModel with @ApplicationContext. Direct sourceRatingRepository replaced.
    ComparisonViewModel uses GetSourceRatingsMapUseCase. Direct sourceRatingRepository replaced.
    All compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix MainActivity DI and DatabaseSeeder</name>
  <files>
    app/src/main/java/com/newsthread/app/util/DatabaseSeeder.kt
    app/src/main/java/com/newsthread/app/presentation/MainActivity.kt
  </files>
  <action>
    **DatabaseSeeder fix:**
    1. Add `@ApplicationContext` annotation to the `context: Context` constructor parameter:
       ```kotlin
       @Singleton
       class DatabaseSeeder @Inject constructor(
           @ApplicationContext private val context: Context,
           private val repository: SourceRatingRepository
       )
       ```
    2. Add import: `import dagger.hilt.android.qualifiers.ApplicationContext`
    3. No other changes — the class is already @Singleton and @Inject

    **MainActivity fix:**
    1. Add Hilt field injection: `@Inject lateinit var databaseSeeder: DatabaseSeeder`
    2. Replace the entire manual construction block in `onCreate()`:
       ```kotlin
       // REMOVE THIS:
       val db = AppDatabase.getDatabase(this@MainActivity)
       val repository = SourceRatingRepositoryImpl(db.sourceRatingDao())
       val seeder = DatabaseSeeder(this@MainActivity, repository)
       val count = seeder.seedSourceRatings()

       // REPLACE WITH:
       val count = databaseSeeder.seedSourceRatings()
       ```
    3. Keep the try/catch and logging around the seed call
    4. Remove unused imports: `AppDatabase`, `SourceRatingRepositoryImpl`, `DatabaseSeeder` constructor import (keep the class import for the lateinit property)
    5. Verify the `@AndroidEntryPoint` annotation is present (it already is)
  </action>
  <verify>
    Run `./gradlew assembleDebug 2>&1 | Select-String -Pattern "BUILD"` — should show "BUILD SUCCESSFUL".
    Grep MainActivity for manual construction: `Select-String -Path app/src/main/java/com/newsthread/app/presentation/MainActivity.kt -Pattern "AppDatabase.getDatabase|SourceRatingRepositoryImpl"` — should return nothing.
    Grep MainActivity for injection: `Select-String -Path app/src/main/java/com/newsthread/app/presentation/MainActivity.kt -Pattern "@Inject.*databaseSeeder"` — should find the injection.
    Grep DatabaseSeeder for annotation: `Select-String -Path app/src/main/java/com/newsthread/app/util/DatabaseSeeder.kt -Pattern "@ApplicationContext"` — should find the annotation.
    Run all tests: `./gradlew test 2>&1 | Select-String -Pattern "BUILD"` — should show "BUILD SUCCESSFUL".
  </verify>
  <done>
    MainActivity no longer manually constructs AppDatabase, SourceRatingRepositoryImpl, or DatabaseSeeder.
    DatabaseSeeder has @ApplicationContext annotation for proper Hilt injection.
    All existing tests pass. App builds successfully. No behavior changes.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` — full build passes
2. `./gradlew test` — all existing unit tests pass
3. No ViewModel contains `sourceRatingRepository` as a direct dependency
4. FeedViewModel has no `userPreferencesRepository` (dead dependency removed)
5. TrackingViewModel extends `ViewModel`, not `AndroidViewModel`
6. MainActivity uses `@Inject lateinit var databaseSeeder` — no manual construction
7. DatabaseSeeder has `@ApplicationContext` annotation
</verification>

<success_criteria>
- All 3 ViewModels follow pragmatic rule: UseCases for domain logic, direct repos for simple reads
- TrackingViewModel is a plain ViewModel with Hilt-injected context
- MainActivity dependency injection is fully handled by Hilt
- All tests pass, app builds and runs correctly
- No behavior changes — purely structural refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/12-architecture-refactor/12-02-SUMMARY.md`
</output>

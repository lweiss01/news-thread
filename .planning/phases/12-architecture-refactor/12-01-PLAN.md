---
phase: 12-architecture-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/newsthread/app/domain/usecase/FilterArticlesUseCase.kt
  - app/src/main/java/com/newsthread/app/domain/usecase/ClusterArticlesUseCase.kt
  - app/src/main/java/com/newsthread/app/domain/usecase/GetSourceRatingsMapUseCase.kt
  - app/src/main/java/com/newsthread/app/domain/usecase/ToggleFollowUseCase.kt
  - app/src/main/java/com/newsthread/app/data/repository/ArticleMappers.kt
  - app/src/main/java/com/newsthread/app/data/repository/NewsRepository.kt
  - app/src/main/java/com/newsthread/app/presentation/feed/FeedScreen.kt
  - app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt
autonomous: true

must_haves:
  truths:
    - "filterArticles logic exists as a standalone UseCase callable from any layer"
    - "clusterArticles logic exists as a standalone UseCase callable from any layer"
    - "Source ratings map building logic exists in one place (GetSourceRatingsMapUseCase), not duplicated in 3 ViewModels"
    - "ToggleFollowUseCase encapsulates check-tracked-state + follow/unfollow branching"
    - "Mapper extensions live in their own file, not at the bottom of NewsRepository"
    - "FeedViewModel is in its own file, separate from FeedScreen composable"
    - "App builds successfully with ./gradlew assembleDebug"
  artifacts:
    - path: "app/src/main/java/com/newsthread/app/domain/usecase/FilterArticlesUseCase.kt"
      provides: "Article quality filtering by source rating allowlist"
      contains: "class FilterArticlesUseCase"
    - path: "app/src/main/java/com/newsthread/app/domain/usecase/ClusterArticlesUseCase.kt"
      provides: "Feed deduplication via Jaccard similarity"
      contains: "class ClusterArticlesUseCase"
    - path: "app/src/main/java/com/newsthread/app/domain/usecase/GetSourceRatingsMapUseCase.kt"
      provides: "Shared source ratings map (domain, sourceId, displayName → SourceRating)"
      contains: "class GetSourceRatingsMapUseCase"
    - path: "app/src/main/java/com/newsthread/app/domain/usecase/ToggleFollowUseCase.kt"
      provides: "Toggle follow/unfollow with state check"
      contains: "class ToggleFollowUseCase"
    - path: "app/src/main/java/com/newsthread/app/data/repository/ArticleMappers.kt"
      provides: "CachedArticleEntity.toDomain(), Article.toEntity(), SourceRatingEntity.toDomain()"
    - path: "app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt"
      provides: "FeedViewModel + FeedUiState extracted from FeedScreen.kt"
      contains: "class FeedViewModel"
  key_links:
    - from: "NewsRepository.getTopHeadlines"
      to: "FilterArticlesUseCase + ClusterArticlesUseCase"
      via: "UseCase injection into NewsRepository"
      pattern: "filterArticlesUseCase|clusterArticlesUseCase"
    - from: "ArticleMappers.kt"
      to: "NewsRepository.kt"
      via: "import of extension functions"
      pattern: "import.*ArticleMappers"
---

<objective>
Extract business logic from NewsRepository into Domain UseCases, move mapper extensions to a dedicated file, and split FeedViewModel into its own file.

Purpose: Separate concerns so business logic (filtering, clustering, source rating maps, follow toggling) lives in the Domain layer as reusable UseCases — not embedded in repositories or ViewModels.

Output: 4 new UseCase files, 1 mapper file, 1 extracted ViewModel file, and a slimmed-down NewsRepository focused on data access only.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-architecture-refactor/12-CONTEXT.md
@.planning/phases/12-architecture-refactor/12-RESEARCH.md
@app/src/main/java/com/newsthread/app/data/repository/NewsRepository.kt
@app/src/main/java/com/newsthread/app/domain/usecase/FollowStoryUseCase.kt
@app/src/main/java/com/newsthread/app/presentation/feed/FeedScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Domain UseCases</name>
  <files>
    app/src/main/java/com/newsthread/app/domain/usecase/FilterArticlesUseCase.kt
    app/src/main/java/com/newsthread/app/domain/usecase/ClusterArticlesUseCase.kt
    app/src/main/java/com/newsthread/app/domain/usecase/GetSourceRatingsMapUseCase.kt
    app/src/main/java/com/newsthread/app/domain/usecase/ToggleFollowUseCase.kt
  </files>
  <action>
    Create 4 new UseCase files following the existing pattern in `FollowStoryUseCase.kt` (`@Inject constructor`, `operator fun invoke`):

    **FilterArticlesUseCase:**
    - Move the `filterArticles` logic from `NewsRepository` (lines 231-256)
    - `operator fun invoke(articles: List<Article>, allowedSources: List<SourceRating>): List<Article>`
    - Keep the tri-match logic (ID, name, domain) exactly as-is
    - Inject nothing — this is a pure function UseCase (stateless)

    **ClusterArticlesUseCase:**
    - Move the `clusterArticles` logic from `NewsRepository` (lines 258-313)
    - `operator fun invoke(articles: List<Article>): List<Article>`
    - Keep Jaccard similarity with same thresholds (0.2 same-source, 0.45 cross-source)
    - Keep stop words list exactly as-is
    - Inject nothing — pure function UseCase (stateless)

    **GetSourceRatingsMapUseCase:**
    - `@Inject constructor(private val sourceRatingRepository: SourceRatingRepository)`
    - `suspend operator fun invoke(): Map<String, SourceRating>`
    - Build the multi-key lookup map: for each rating, add entries for `domain`, `sourceId`, and `displayName` (only if non-blank)
    - This is the logic currently duplicated in FeedViewModel.loadSourceRatings(), TrackingViewModel.loadSourceRatings(), and ComparisonViewModel.loadSourceRatings()
    - Return a one-shot Map (not a Flow — ratings are static per session, per CONTEXT.md decision)

    **ToggleFollowUseCase:**
    - `@Inject constructor(private val trackingRepository: TrackingRepository, private val followStoryUseCase: FollowStoryUseCase)`
    - `suspend operator fun invoke(article: Article, currentTrackedMap: Map<String, String>)`
    - If `currentTrackedMap[article.url]` is not null → call `trackingRepository.unfollowStory(storyId)`
    - Else → call `followStoryUseCase(article)`
    - This extracts the branching logic from FeedViewModel.toggleFollow() (lines 187-196)
  </action>
  <verify>
    Run `./gradlew compileDebugKotlin 2>&1 | Select-String -Pattern "error"` — should show 0 errors for the new files.
    Grep each new file: `Get-Content app/src/main/java/com/newsthread/app/domain/usecase/FilterArticlesUseCase.kt | Select-String "class FilterArticlesUseCase"` — confirms class exists.
    Repeat for all 4 UseCase files.
  </verify>
  <done>4 new UseCase files compile successfully with @Inject constructors and operator fun invoke signatures.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Mappers and Split FeedViewModel</name>
  <files>
    app/src/main/java/com/newsthread/app/data/repository/ArticleMappers.kt
    app/src/main/java/com/newsthread/app/data/repository/NewsRepository.kt
    app/src/main/java/com/newsthread/app/presentation/feed/FeedScreen.kt
    app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt
  </files>
  <action>
    **Step A: Create ArticleMappers.kt**
    - Move the 3 mapper extension functions from NewsRepository.kt (lines 322-385) to a new file `ArticleMappers.kt` in the same package (`data.repository`)
    - `CachedArticleEntity.toDomain(): Article`
    - `Article.toEntity(now: Long): CachedArticleEntity`
    - `SourceRatingEntity.toDomain(): SourceRating`
    - Change visibility from `private` to `internal` so NewsRepository can still call them
    - Add proper imports (Article, Source, CachedArticleEntity, SourceRatingEntity, SourceRating, CacheConstants)

    **Step B: Update NewsRepository.kt**
    - Remove the mapper extension functions (lines 319-385)
    - Remove `filterArticles` method (lines 231-256)
    - Remove `clusterArticles` method (lines 258-313)
    - Inject `FilterArticlesUseCase` and `ClusterArticlesUseCase` in the constructor
    - Update `getTopHeadlines()` to call the injected UseCases instead of private methods:
      - `cached = filterArticlesUseCase(cached, ratedSources)`
      - `cached = clusterArticlesUseCase(cached)`
      - Same for the network path (lines 99-103)
    - Add import for `ArticleMappers` extensions (they're now in the same package so `toDomain()` and `toEntity()` should resolve with an import of the file)

    **Step C: Split FeedViewModel out of FeedScreen.kt**
    - Create new file `app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt`
    - Move `FeedUiState` sealed interface (lines 69-73) and `FeedViewModel` class (lines 75-197) to the new file
    - Add all necessary imports to FeedViewModel.kt
    - In FeedScreen.kt, remove lines 69-197 (the ViewModel and UiState)
    - Add import for `FeedViewModel` and `FeedUiState` in FeedScreen.kt
    - Clean up unused imports in FeedScreen.kt (remove ViewModel, viewModelScope, MutableStateFlow, etc.)
  </action>
  <verify>
    Run `./gradlew assembleDebug 2>&1 | Select-String -Pattern "BUILD"` — should show "BUILD SUCCESSFUL".
    Verify file exists: `Test-Path app/src/main/java/com/newsthread/app/data/repository/ArticleMappers.kt` — True.
    Verify FeedViewModel extracted: `Test-Path app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt` — True.
    Verify NewsRepository no longer has filter/cluster: `Select-String -Path app/src/main/java/com/newsthread/app/data/repository/NewsRepository.kt -Pattern "private fun filterArticles|private fun clusterArticles"` — should return nothing.
    Verify existing tests pass: `./gradlew test 2>&1 | Select-String -Pattern "BUILD"` — should show "BUILD SUCCESSFUL".
  </verify>
  <done>
    NewsRepository contains only data access logic (fetch, cache, emit).
    Mapper extensions live in ArticleMappers.kt.
    FeedViewModel is in its own file with FeedUiState.
    All existing tests pass. App builds successfully.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` — full build passes
2. `./gradlew test` — all existing unit tests pass (ClusteringUnitTest, TrackingRepositoryTest, etc.)
3. NewsRepository.kt line count decreased significantly (removed ~100 lines of filter/cluster/mapper code)
4. No references to `private fun filterArticles` or `private fun clusterArticles` in NewsRepository.kt
5. FeedScreen.kt no longer contains `class FeedViewModel`
6. 4 new UseCase files exist in `domain/usecase/` directory
</verification>

<success_criteria>
- Business logic extracted from NewsRepository into domain UseCases
- Mapper extensions in dedicated file
- FeedViewModel in its own file
- All tests pass
- App builds successfully
- No behavior changes — all logic is identical, just relocated
</success_criteria>

<output>
After completion, create `.planning/phases/12-architecture-refactor/12-01-SUMMARY.md`
</output>

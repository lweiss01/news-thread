---
id: "09.5-04"
name: "Stability & Verification (Recovery)"
wave: "2"
depends_on: ["09.5-01", "09.5-02", "09.5-03"]
files_snapshot:
  - "app/src/main/java/com/newsthread/app/presentation/tracking/TrackingScreen.kt"
  - "app/src/main/java/com/newsthread/app/presentation/feed/FeedViewModel.kt"
  - "app/src/main/java/com/newsthread/app/data/repository/NewsRepository.kt"
---

<objective>
Recover and stabilize Phase 9.5 features following a mid-phase handoff. The goal is to verify existing implementation and fix reported bugs in Feed Clustering, UI Badges, and Untrack functionality.
</objective>

<context>
Work on Phase 9.5-05 was paused mid-execution. A `.continue-here.md` file indicates that while code for Feed Clustering and UI updates was written, it is "buggy" and "not working as intended". Specifically:
1.  **Feed Clustering**: Users report it is not working (duplicates or bad groups).
2.  **UI Badges**: Reliability shields may be missing or incorrect.
3.  **Untrack**: Functionality needs verification.

This plan consolidates the verification and fix work required to close Phase 9.5.
</context>

<tasks>
  <task id="verify_clustering" name="Verify & Fix Feed Clustering">
    <description>
      Debug `NewsRepository.clusterArticles`.
      - Check Jaccard similarity threshold (currently 0.2).
      - Verify "stop words" list effectiveness.
      - Ensure clustered articles are actually grouped or duplicates removed as expected.
    </description>
    <action type="verify_and_modify" path="app/src/main/java/com/newsthread/app/data/repository/NewsRepository.kt">
      <step>Review `clusterArticles` logic.</step>
      <step>Create unit test `NewsRepositoryTest` to reproduce clustering failure if possible.</step>
    </action>
  </task>

  <task id="verify_ui_badges" name="Verify Source Badges">
    <description>
      Confirm `ArticleCard` correctly displays `ReliabilityBadge`.
      - Check `FeedViewModel` mapping of `sourceRatings`.
      - Ensure `findRatingForArticle` logic in `ArticleCard` works for domain/ID matching.
    </description>
    <action type="verify" path="app/src/main/java/com/newsthread/app/presentation/common/ArticleCard.kt" />
  </task>

  <task id="verify_untrack" name="Verify Untrack Functionality">
    <description>
      Test bookmark toggle in Feed and Tracking screens.
      - Ensure clicking "Unfollow" updates local DB and UI state immediately.
    </description>
    <action type="verify" path="app/src/main/java/com/newsthread/app/presentation/tracking/TrackingScreen.kt" />
  </task>
</tasks>

<verification>
  <manual_test>
    1. **Clustering**: Launch app -> Refresh Feed. Observe if duplicate stories (e.g. "Same Title") are grouped or hidden.
    2. **Badges**: Scroll feed. Confirm shields appear next to source names.
    3. **Untrack**: Go to Tracking tab -> Tap Bookmark on a story -> Confirm it disappears or toggle updates.
  </manual_test>
</verification>

---
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - app/src/main/java/com/newsthread/app/ui/components/ArticleCard.kt
  - app/src/main/java/com/newsthread/app/ui/screens/feed/FeedScreen.kt
  - app/src/main/java/com/newsthread/app/ui/screens/story/StoryDetailScreen.kt
  - app/src/main/java/com/newsthread/app/ui/MainActivity.kt
autonomous: true
---

# Plan 13-03: Card & Feed Refinements

Implement the premium UI refinements for the news feed and story detail screens, including bias accents, deep-linking bars, glassmorphism, and haptic feedback.

## Tasks

<task>
### Implement 3px Left-Border Accents
Add a solid 3px left border to `ArticleCard` (used in Perspective sections) that matches the article's bias color.
**Validation:** Check `ArticleCard.kt` for `Modifier.drawBehind` or `Modifier.border` logic with a specific width of `3.dp`.
</task>

<task>
### Add Slim Bias Bar to Feed Cards
Implement the slim `BiasHeatmap` bar at the bottom of feed cards. Ensure it is tappable and triggers navigation to the `StoryDetailScreen`.
**Validation:** Check `FeedScreen.kt` / `ArticleCard.kt` for a `BiasHeatmap` component with a `Modifier.clickable` that calls the story navigation lambda.
</task>

<task>
### Implement Glassmorphism Effects
Apply subtle blur/transparency effects to `BottomSheets` and `Dialogs` for an "Inverted" premium aesthetic.
**Validation:** Verify usage of `Modifier.blur` or custom `GlassMorphismContainer` logic in `StoryDetailScreen` or navigation host.
</task>

<task>
### Add Haptic Feedback Ticks
Integrate `LocalHapticFeedback` into the Bias Bar interaction and the Bookmark toggle.
**Validation:** Search for `haptic.performHapticFeedback(HapticFeedbackType.TextHandleMove)` or similar in `ArticleCard.kt`.
</task>

<task>
### Invert Toast & Notification Aesthetic
Update the Toast pill to follow the light-on-dark (Dark Mode) or dark-on-light (Light Mode) aesthetic with `Amber500` icon backgrounds.
**Validation:** Check `NotificationHelper.kt` or `ToastComponent` for background color logic that varies by theme.
</task>

## Must-Haves
- 3px solid bias accents on perspective cards.
- Tappable bias bar on feed cards (deep-links to analysis).
- Glassmorphism on overlays.
- Haptic feedback on key interactions.
- "Inverted" aesthetic for notifications/dialogs.

---
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/src/main/res/drawable/ic_shield_high.xml
  - app/src/main/res/drawable/ic_shield_medium.xml
  - app/src/main/res/drawable/ic_shield_low.xml
  - app/src/main/res/drawable/ic_shield_unrated.xml
  - app/src/main/java/com/newsthread/app/ui/components/SourceShield.kt
autonomous: true
---

# Plan 13-01: Redesigned Shield System

Implement the new "Softened" credibility shield system, replacing the existing security-focused icons with professional editorial indicators.

## Tasks

<task>
### Import Redesigned Shield Vectors
Import the modern shield XML files from the downloads folder (handling the dashed outline preference and path conversion).
**Validation:** Check `res/drawable` for `ic_shield_high`, `ic_shield_medium`, `ic_shield_low`, and `ic_shield_unrated`. Verify they are VectorDrawables.
</task>

<task>
### Update SourceShield Composable
Update the `SourceShield` component to use the new icons and the "Caution" color model (Orange for Low). Ensure labels match the new "Softened" descriptions.
**Validation:** Verify `SourceShield` logic maps `Reliability.LOW` to `ic_shield_low` and `Orange500`.
</task>

## Must-Haves
- Orange shields used for Low reliability.
- Softened shield shapes implemented.
- Unrated shield uses dashed outline (with filled fallback logic).

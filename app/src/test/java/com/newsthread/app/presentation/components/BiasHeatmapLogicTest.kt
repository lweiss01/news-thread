package com.newsthread.app.presentation.components

import org.junit.Assert.assertEquals
import org.junit.Test
import org.junit.Ignore

@Ignore("Fix build: Missing function calculateNormalizedBiasX")
class BiasHeatmapLogicTest {
    /*
    @Test
    fun `calculateNormalizedBiasX_correctlyMapsStandardValues`() {
        // -2 -> 0.10f
        assertEquals(0.1f, calculateNormalizedBiasX(-2), 0.001f)

        // -1 -> 0.30f
        assertEquals(0.3f, calculateNormalizedBiasX(-1), 0.001f)

        // 0 -> 0.50f
        assertEquals(0.5f, calculateNormalizedBiasX(0), 0.001f)

        // 1 -> 0.70f
        assertEquals(0.7f, calculateNormalizedBiasX(1), 0.001f)

        // 2 -> 0.90f
        assertEquals(0.9f, calculateNormalizedBiasX(2), 0.001f)
    }

    @Test
    fun `calculateNormalizedBiasX_clampsOutOfBoundsValues`() {
        // -3 -> clamped to -2 -> 0.10f
        assertEquals(0.1f, calculateNormalizedBiasX(-3), 0.001f)

        // 3 -> clamped to 2 -> 0.90f
        assertEquals(0.9f, calculateNormalizedBiasX(3), 0.001f)

        // 100 -> clamped to 2 -> 0.90f
        assertEquals(0.9f, calculateNormalizedBiasX(100), 0.001f)
    }
    */
}
